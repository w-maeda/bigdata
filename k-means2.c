#include <stdio.h>
#include <string.h>
#include <stdlib.h>
#include <time.h>
#define MAX 256

int main(void){
  int x,y,z;
  int i,l,k;
  int cluster_MAX;
  int cluster[MAX];
  int max,min;

  printf("数を入力してください\n");
  printf("x=");
  scanf("%d",&x);
  printf("y=");
  scanf("%d",&y);
  printf("クラスタ数:");
  scanf("%d",&z);
  printf("最大:");
  scanf("%d",&max);
  printf("最小:");
  scanf("%d",&min);

  cluster_MAX=z*z;//クラスタ分割の前準備

  for(i=1;i<=x;i++){

  }

}
